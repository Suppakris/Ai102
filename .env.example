# ─────────────────────────────────────────────────────────────
# Ai102 — environment variables
# Copy this file to `.env` (local) or add these in Vercel →
# Settings → Environment Variables (Production + Preview).
# ─────────────────────────────────────────────────────────────

# ══ REQUIRED — the app will not build or run without these ══════

# Postgres connection string. Free: https://neon.tech (create a
# project, copy the "Connection string"). Must be a full postgres:// URL.
DATABASE_URL=""

# Session signing key. Generate one:  openssl rand -base64 32
NEXTAUTH_SECRET=""

# The deployed URL (no trailing slash).
# Local:  http://localhost:3000
# Vercel: https://<your-project>.vercel.app
NEXTAUTH_URL=""

# Google OAuth login. Google Cloud Console → APIs & Services →
# Credentials → Create OAuth client ID (type: Web application).
# Authorized redirect URI must be:
#   <NEXTAUTH_URL>/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ══ OPTIONAL — leave blank; features degrade gracefully ════════

# LLM key. NOT required: users bring their own key in the app's
# Settings → "AI Provider & API Key" (Groq's free tier works).
# Only set this to force a server-wide default key.
OPENAI_API_KEY=""

# AI image generation (Together AI has a free FLUX model). Without
# it, image slides fall back to stock/no-image.
TOGETHER_AI_API_KEY=""

# Image storage for AI-generated images (https://uploadthing.com).
UPLOADTHING_TOKEN=""

# Stock photos (https://unsplash.com/developers).
UNSPLASH_ACCESS_KEY=""

# Web search for outlines (https://tavily.com).
TAVILY_API_KEY=""
